{
  "name": "task-manager",
  "version": "1.0.0",
  "description": "Full-stack task manager: React + Vite frontend, Spring Boot backend",
  "scripts": {
    "frontend:dev": "cd frontend && npm run dev",
    "frontend:build": "cd frontend && npm run build",
    "frontend:lint": "cd frontend && npm run lint",
    "frontend:preview": "cd frontend && npm run preview",
    "backend:run": "cd backend && mvn spring-boot:run",
    "backend:build": "cd backend && mvn clean install",
    "dev": "echo 'Start backend: npm run backend:run' && echo 'Start frontend: npm run frontend:dev'"
  }
}
